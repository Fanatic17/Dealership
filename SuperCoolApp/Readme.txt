Readme


Per avviare il progetto, dopo aver clonato da git i sorgenti, aprire su Visual Studio il progetto e caricare la soluzione 'SuperCoolApp'.
Fare tasto destro sulla soluzione e selezionare "Imposta progetti di avvio". 
Selezionare "Service API" e "SuperCoolApp" in Progetti di avvio multipli.


Avviare XAMPP sul proprio computer ed avviare MySQL.

///Eseguire solo per la prima esecuzione del progetto
///Avviare Apache su XAMPP e cliccare admin su MySql e controllare che non siano già presenti database col nome "aquinoconsoli".
///Cliccare sul tasto "Play" (Avvia) ed eseguire il progetto. Si aprirà una finestra di Chrome in cui sarà presente un link da premere(setup)
///per configurare il database in modo corretto. Quando il database sarà creato comparirà il messaggio "database created".


Aprire un browser e digitare nella barra degli indirizzi: localhost:56000

A questo punto sarà possibile utilizzare le funzionalità del progetto che consistono nella gestione di utenti e veicoli di una concessionaria.