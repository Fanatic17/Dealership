Readme


To start the project, after getting the code from git, open it in Visual Studio and load the solution "SuperCoolApp".
Select "Service API" e "SuperCoolApp" in the Multiple Startup Project option.


Load XAMPP and start MySQL.

///Only for the first execution
///Check for tables with the name "ConsoliDAquino" in PHPMyAdmin.
///Run the project. It will open a browser window with a button. Clicking that button will create the database.When the operation completes will be displayed the message "database created".


Open a browser and enter: localhost:56000

